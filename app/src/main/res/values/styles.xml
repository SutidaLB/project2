<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- กำหนดรูปแบบต่างๆ ของ Text บนแท็บ -->
    <style name="MyTabTextStyle" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textColor">#ffff99</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!-- รูปแบบอื่นๆ ต้องกำหนดแยกจาก Text -->
    <style name="MyTabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyTabTextStyle</item>
        <item name="tabIndicatorColor">#ff0000</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="android:background">#abc</item>
        <item name="tabSelectedTextColor">#ffffff</item>
    </style>

</resources>
